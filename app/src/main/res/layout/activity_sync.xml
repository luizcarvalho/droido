<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="br.com.redrails.torpedos.parse.SyncActivity"
    android:id="@+id/activity_sync"
    >

<Button
    android:id="@+id/sync_action_button"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/sync_db_button_text"


    />

    <TextView
        android:id="@+id/sync_result"
        android:layout_below="@+id/sync_action_button"
        android:text="@string/sync_default_result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />


    <com.google.android.gms.ads.AdView
        xmlns:ads="http://schemas.android.com/apk/res-auto"
        android:id="@+id/adView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        ads:adUnitId="ca-app-pub-6662263716470706/5987486187"
        ads:adSize="SMART_BANNER"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>
